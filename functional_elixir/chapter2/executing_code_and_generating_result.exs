# ex> "Hello, World!" + 5
# ** (ArithmeticError) bad argument in arithmetic expression
# :erlang.+("Hello, World!", 5)
# => that's not javascript bro
#
# Operators
# Operator - useful for -  examples
# + Adding numbers 10 + 5 , 3.7 + 8.1
# - Subtracting numbers 10 - 25 , 9.7 - 8.1
# / Dividing numbers 10 / 2 , 0 / 10
# * Multiplying numbers 10 * 2 , 0 * 10
# == Checking when two values are equal 1 == 1.0 , 1 == 2
# != Checking when two values are not equal 1 != 1.0 , 1 != 2
# < Checking when the left value is less than the
# right one 1 < 2 , 2 < 1
# > Checking when the left value is greater than
# the right one 1 > 2 , 2 > 1
# ++ Concatenating two lists [1, 2] ++ [3, 4]
# <> Concatenating two strings or binaries "Hello, " <> "World"
